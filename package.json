{
  "name": "baiboly",
  "version": "1.0.0",
  "private": true,
  "description": "Baiboly — Malagasy Bible App",
  "scripts": {
    "build:data": "node scripts/build-data.js",
    "dev": "node scripts/dev-server.js",
    "start": "npm run build:data && npm run dev"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/cli": "^6.2.1",
    "@capacitor/core": "^6.2.1"
  }
}
